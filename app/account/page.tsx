import { ServicePage } from "@/components/pupsart"; export default function Page(){return <ServicePage title="Account — Coming soon"/>}

export const metadata = { title: 'Account', robots: { index: false, follow: true } };
