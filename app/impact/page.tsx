import { pageMetadata } from "@/lib/seo";
import { ImpactPage } from "@/components/pupsart"; export default function Page(){return <ImpactPage/>}

export const metadata = pageMetadata("Impact: il progetto per il benessere animale", "Scopri il progetto Pupsart Impact: una rete in costruzione per sostenere il benessere animale con criteri, partner e risultati trasparenti.", "/impact");
